[package]
name = "{{project}}"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "{{description}}"
repository = "https://github.com/tzervas/{{project}}"
readme = "README.md"
keywords = ["ml", "training", "inference"]
categories = ["science"]

[dependencies]
# Add candle / torch-sys / ndarray as needed for the product crate.
# Donors: qlora-rs, peft-rs, unsloth-rs, bitnet-rs, trit-vsa

[dev-dependencies]
